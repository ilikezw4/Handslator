import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'at.htlhl.Handslator',
  appName: 'Handslator',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
